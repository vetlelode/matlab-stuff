function [res] = Oppgave6(k)
    for i = 1:k
        if mod(i,2) ~= 0
            i = i + 1;
            if mod(i,2) ~= 0
                res = i
            else 
                res = i - 1
            end
        end
    end
end